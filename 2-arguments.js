const { argv } = require('node:process');

if (parseArgs.length === 0) {
    console.log("No arguments");
}   else if (parseArgs.length === 1) {
    console.log("Arguments found");
}   else {
    console.log("Arguments found");
}